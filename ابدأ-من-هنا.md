# ابدأ من هنا — دليل تشغيل نظام سُحُب خطوة بخطوة

> هذا الدليل مكتوب لغير المبرمجين. كل خطوة مكتوب فيها: **أين تضغط**، و**ماذا تكتب**، و**ماذا ستشاهد**.
> خُذ وقتك، ولا تنتقل لخطوة قبل أن تنتهي من التي قبلها. وإذا ظهر لك شيء غريب، ارجع لآخر قسم في هذا الملف («إذا ظهرت لي رسالة…»).

---

## قبل أن نبدأ: الصورة الكبيرة (اقرأها مرة واحدة فقط)

سنعمل **٤ أمور** بالترتيب:

| # | ماذا نعمل | لماذا |
| --- | --- | --- |
| ١ | نجهّز **قاعدة بيانات** على Neon | مكان تخزين المنتجات والفواتير |
| ٢ | نرفع ملفات المشروع إلى **GitHub** | مكان ملفات الكود، ومنه يأخذ Vercel الكود |
| ٣ | نشغّل الموقع على **Vercel** ونربطه بقاعدة Neon | حتى يصبح النظام متاحًا على الإنترنت |
| ٤ | نفتح **صفحة تهيئة** لمرة واحدة | تصنع الجداول وتصنع حساب المدير |

الوقت المتوقّع: **٢٠–٣٠ دقيقة**. لا تحتاج تثبيت أي برنامج على جهازك.

**احفظ هذه المعلومات في ورقة أو في ملاحظات هاتفك:**

```
اسم المستودع على GitHub : sohob-liquids
رمز التهيئة (SETUP_TOKEN): Sohob-Setup-2026
حساب الدخول الأول       : admin
كلمة المرور الأولية     : admin123
```

---

## المرحلة ١ — قاعدة البيانات على Neon

### ١-١ إنشاء حساب
1. افتح المتصفح واكتب العنوان: `neon.com`
2. اضغط زر **Sign up** (أعلى يمين الصفحة).
3. سجّل بالطريقة الأسهل لك:
   - **Continue with GitHub** (إن عملت حساب GitHub في المرحلة ٢ أولًا)، أو
   - بالبريد الإلكتروني + كلمة مرور.
4. إن سجّلت بالبريد: افتح بريدك واضغط رابط التأكيد الذي وصلَك.

### ١-٢ إنشاء المشروع (Database Project)
1. بعد الدخول ستظهر لك صفحة **Create your project** (أو اضغط **New Project**).
2. املأ الحقول:
   - **Project name**: اكتب `sohob` (أي اسم يعجبك).
   - **Postgres version**: اتركها كما هي.
   - **Region**: اختر أقرب منطقة لك. لمصر/الشام/الخليج اختر `AWS eu-central-1` أو `AWS eu-west-2`. (هذا يسرّع النظام كثيرًا).
3. اضغط **Create project** وانتظر ٥–١٥ ثانية.

### ١-٣ نسخ رابط الاتصال (أهم خطوة في المرحلة)
1. بعد الإنشاء ستظهر لك لوحة فيها قسم اسمه **Connection string** (أو اضغط **Connect** أعلى الصفحة).
2. تأكد أن الاختيار هو **Connection string** (وليس psql أو أي شيء آخر).
3. ستجد في الأعلى خيارين:
   - **Pooled connection** ← اختر هذا (يكون في الرابط كلمة `-pooler`)
   - **Direct connection**
   > المطلوب الآن: **Pooled connection**.
4. اضغط زر **Copy** ⧉ بجانب الرابط. الرابط شكله مثل:
   ```
   postgresql://neondb_owner:AbCdEf123456@ep-cool-name-123456-pooler.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```
5. **الصقه الآن في ملف ملاحظات عندك** مع باقي المعلومات، وسمّه: «رابط قاعدة البيانات (Pooled)».

> ️ ثلاث تنبيهات مهمة:
> 1. هذا الرابط فيه **كلمة مرور قاعدة بياناتك** — لا ترسله لأي شخص ولا تضعه في أي مكان عام (لا في GitHub، ولا في صفحة الويب).
> 2. الرابط يجب أن يبدأ بـ `postgresql://` — إن نسخت شيئًا لا يبدأ بهذا، ارجع وانسخ من جديد.
> 3. احتفظ بنسخة من الرابط في مكان آمن؛ ستحتاجه في المرحلة ٣.

✅ **انتهت المرحلة ١.** عندك الآن قاعدة بيانات فارغة + رابطها في ملاحظاتك.

---

## المرحلة ٢ — رفع الملفات إلى GitHub

### ٢-١ إنشاء حساب GitHub (إن لم يكن عندك)
1. افتح `github.com` واضغط **Sign up**.
2. أدخل بريدك، وكلمة مرور، واسم مستخدم (مثلًا `haitham-sohob`).
3. أكّد البريد من الرسالة التي تصل إليه.

### ٢-٢ إنشاء مستودع (Repository) جديد
1. بعد الدخول، اضغط علامة **+** أعلى يمين الصفحة ← **New repository**.
2. املأ:
   - **Repository name**: `sohob-liquids`
   - **Description** (اختياري): `نظام إدارة مبيعات ومخزون الليكويد`
   - اختر **Private**  (مهم — لا تختر Public).
   - لا تُفعّل أي خيار من Add README / .gitignore / license (اتركها فارغة).
3. اضغط **Create repository**.

### ٢-٣ رفع الملفات (بدون أي برامج)
1. في الصفحة الجديدة ستجد رابطًا نصيًا: **uploading an existing file** — اضغطه.
2. **قبل السحب**: افتح مجلد المشروع على جهازك، وهو:
   `C:\Users\Haitham\Downloads\e-liquid-sales-management-system`

   ثم احذف منه المجلد التالي (تجنّبًا لأخطاء الرفع — وهو غير ضروري):
   - `node_modules` ← اضغط عليه بزر الفأرة الأيمن ← **Delete**

3. الآن اسحب الملفات إلى صفحة GitHub (السحب والإفلات). المطلوب **بالضبط**:

   **مجلدان:**
   - `src`
   - `public`

   **وملفات (واحدة واحدة):**
   - `package.json`
   - `package-lock.json`
   - `next.config.ts`
   - `tsconfig.json`
   - `postcss.config.mjs`
   - `eslint.config.mjs`
   - `drizzle.config.ts`
   - `README.md`
   - `ابدأ-من-هنا.md` (هذا الملف)
   - `.gitignore`
   - `.env.example`

   > ⛔ **لا ترفع أبدًا**: مجلد `node_modules`، ولا مجلد `.next` (لو موجود)، ولا أي ملف اسمه `.env.local`.
   > 💡 لو لم تجد الملفَّين `.gitignore` و `.env.example` في المجلد: في نافذة المجلد اضغط **View ← Show ← Hidden items** (عرض ← إظهار ← العناصر المخفية). إن لم يظهرا فلا مشكلة، أكمل الباقي.

4. في أسفل الصفحة ستجد خانة **Commit changes**، اكتب فيها `أول رفع للمشروع` ثم اضغط الزر الأخضر **Commit changes**.
5. انتظر انتهاء الرفع (شريط تقدّم). عند النجاح سترى ملفات المشروع أمامك على GitHub.

✅ **انتهت المرحلة ٢.**

---

## المرحلة ٣ — تشغيل الموقع على Vercel وربطه بالبيانات

### ٣-١ إنشاء حساب Vercel
1. افتح `vercel.com` واضغط **Sign Up**.
2. اختر **Continue with GitHub** (هذه الأسهل، لأننا رفعنا الكود على GitHub).
3. عند سؤال Vercel عن السماح بقراءة مستودعاتك: اضغط **Authorize**.

### ٣-٢ ربط المستودع
1. في لوحة Vercel اضغط **Add New…** ← **Project**.
2. ستظهر قائمة مستودعاتك، اضغط **Import** أمام المستودع `sohob-liquids`.
   - إن لم يظهر: اضغط **Adjust GitHub App Permissions** ← اختر `sohob-liquids` ← Save.
3. في الصفحة التالية:
   - **Project Name**: `sohob-liquids` (اتركه كما هو).
   - **Framework Preset**: سيكتب تلقائيًا `Next.js` — **اتركه ولا تغيّره**.
   - **Build and Output Settings**: **لا تلمسها** (اتركها مغلقة كما هي افتراضيًا).

### ٣-٣ إضافة متغيّرات البيئة (هنا نربط قاعدة البيانات)
1. في نفس الصفحة، انزل إلى قسم **Environment Variables**. أغلب الظن أنه مغلق — افتحه بالضغط عليه.
2. سنضيف **قيمتين**. لكل واحدة:
   - اكتب الاسم في خانة **Key**
   - الصق القيمة في خانة **Value**
   - اترك مربّعات **Production و Preview و Development** محدّدة (كلها)
   - اضغط **Add**

   **القيمة الأولى:**
   ```
   Key   : DATABASE_URL
   Value : (الصق هنا رابط Neon المجمّع الذي نسخته في المرحلة ١ — الذي فيه -pooler)
   ```

   **القيمة الثانية:**
   ```
   Key   : SETUP_TOKEN
   Value : Sohob-Setup-2026
   ```

   > ⚠️ في خانة Value: الصق الرابط كاملًا **بدون** علامات تنصيص وبدون مسافات زائدة.

3. بعد الإضافة ستشاهد الاسمين في القائمة. تأكد من كتابة الاسمين بالحرف الكبير تمامًا كما هو مكتوب أعلى.

### ٣-٤ النشر
1. اضغط الزر الأزرق **Deploy** (أسفل الصفحة).
2. ستنتقل لصفحة فيها قائمة خطوات تُنفّذ. انتظر من **دقيقة إلى ثلاث دقائق**.
3. عند النجاح ستظهر لك كلمة **Congratulations** وزر **Continue to Dashboard** — اضغطه.
4. في اللوحة ستجد نطاق موقعك، شكله:
   ```
   https://sohob-liquids.vercel.app
   ```
   **انسخ هذا النطاق** وضعْه في ملاحظاتك. (قد يزيد عليه أرقام، مثل `sohob-liquids-abc.vercel.app` — استخدم الذي يظهر لك).
5. جرّب فتح النطاق في المتصفح الآن: ستظهر لك **صفحة تسجيل الدخول** (تصميم أسود وأخضر، مكتوب فيه «سُحُب»).
   - إن لم تظهر: ارجع للخطوة ٣-٣ وتأكد من المتغيّرين، ثم اضغط **Redeploy** (انظر قسم «إعادة النشر» لاحقًا).

> ️ **لماذا لا أستطيع الدخول الآن؟** لأن الجداول لم تُنشأ بعد، ولا يوجد حساب مدير. هذا عمل المرحلة ٤.

✅ **انتهت المرحلة ٣.**

---

## المرحلة ٤ — تهيئة قاعدة البيانات (مرة واحدة فقط)

1. في المتصفح، افتح الرابط التالي (بدّل `النطاق` بنطاقك من الخطوة ٣-٤):

   ```
   https://النطاق/api/setup?token=Sohob-Setup-2026
   ```

   مثال جاهز إن كان نطاقك `sohob-liquids.vercel.app`:
   ```
   https://sohob-liquids.vercel.app/api/setup?token=Sohob-Setup-2026
   ```

2. اضغط Enter وانتظر ٥–١٥ ثانية (كانت قاعدة Neon نائمة، فتستيقظ الآن).

3. **ماذا يجب أن تشاهد؟** صفحة عربية سوداء مكتوب فيها بالأخضر:
   > **تمت العملية بنجاح ✅**
   > قاعدة البيانات جاهزة، وتم إنشاء حساب المدير: admin / admin123

   ومكتوب أيضًا: `بيانات حساب المدير { "username": "admin", "password": "admin123" }`

   وفي الأسفل زر: **الانتقال إلى صفحة تسجيل الدخول ←**

4. اضغط الزر، ثم سجّل الدخول:
   - اسم المستخدم: `admin`
   - كلمة المرور: `admin123`

5. إن نجح الدخول سترى **لوحة التحكم** مباشرة (ستكون الأرقام كلها أصفارًا — هذا طبيعي، لأن النظام جديد وفارغ).

> إن ظهرت لك رسالة حمراء بدل النجاح: ارجع لقسم «إذا ظهرت لي رسالة…» في الأسفل.

✅ **انتهت المرحلة ٤ — النظام يعمل الآن.**

---

## المرحلة ٥ — تأمين النظام (٥ دقائق، مهمة جدًا)

كلمة المرور `admin123` معروفة لأي شخص، لذلك:

1. من القائمة الجانبية اضغط **الإعدادات**.
2. في قسم الحساب، اختر **تغيير كلمة المرور**.
3. أدخل:
   - كلمة المرور الحالية: `admin123`
   - كلمة المرور الجديدة: **٨ أحرف أو أكثر** (اكتبها في ملاحظاتك فورًا!)
   - تأكيد كلمة المرور الجديدة
4. اضغط **حفظ**.

5. الآن **أغلق صفحة التهيئة نهائيًا**:
   - ارجع إلى Vercel ← مشروعك ← **Settings** ← **Environment Variables**.
   - اضغط القائمة **⋯** بجانب `SETUP_TOKEN` ← **Remove** (أو اضغط **Delete**).
   - اذهب إلى تبويب **Deployments** ← أحدث عملية نشر ← اضغط **⋯** ← **Redeploy** ← ثم **Redeploy** للتأكيد.
   - بعد انتهاء إعادة النشر: جرّب فتح رابط `/api/setup?token=Sohob-Setup-2026` — يجب أن تظهر رسالة تقول إن الصفحة معطّلة. هذا يعني أن النظام صار محميًا. 👍

> **نسيت كلمة المرور لاحقًا؟** لا مشكلة — راجع القسم الأخير «إذا نسيت كلمة المرور».

---

## المرحلة ٦ — إدخال بياناتك الحقيقية

النظام يبدأ فارغًا تمامًا. الترتيب المقترح:

1. **المنتجات** (من القائمة الجانبية ← المنتجات ← زر **إضافة منتج**):
   - الاسم، التصنيف، سعر البيع، التكلفة، الكمية، حد التنبيه (Low stock)، الوصف.
   - يمكنك إضافة **صورة للمنتج** من نفس النافذة (اختر صورة من جهازك).
   - خصائص إضافية (نسبة النيكوتين، حجم الزجاجة، VG/PG…) لها أزرار جاهزة.
2. **العملاء** ← **إضافة عميل**: الاسم، النوع (محل / شركة / فرد)، الهاتف، العنوان.
3. **فاتورة جديدة** ← اختر العميل ← أضف الأصناف والكميات ← نوع الشحن ← **حفظ**.
   - الكميات تُخصم من المخزون تلقائيًا.
4. جرّب **التقارير** و **سجل النشاط** و **لوحة التحكم** لتتابع الأرقام.

لإضافة شخص آخر يستخدم النظام: **المستخدمون ← إضافة مستخدم** (اختر مستخدمًا عاديًا أو مديرًا).

---

## إعادة النشر (Redeploy) — ماذا يعني وكيف أعمله؟

أي تغيير تعمله في **المتغيّرات** أو في **الكود على GitHub** يحتاج إعادة نشر حتى يُطبّق:

1. افتح `vercel.com` ← مشروعك.
2. تبويب **Deployments**.
3. أمام أحدث نشر اضغط **⋯** ← **Redeploy** ← أكّد.

> إن غيّرت ملفًا على GitHub (رفعت ملفًا جديدًا أو عدّلت ملفًا)، Vercel يعيد النشر **تلقائيًا** خلال دقيقة، وتصلك النتيجة في تبويب Deployments.

**كيف أعرف أن النشر نجح؟** تظهر كلمة **Ready** بالأخضر بجانب النشر. وإن ظهرت **Error** بالأحمر، اضغط على النشر وانسخ آخر ١٠ أسطر حمراء وابعتْها لي لأصلحها لك.

---

## إذا ظهرت لي رسالة… (حل سريع)

| ما تشاهده | المعنى | الحل |
| --- | --- | --- |
| **صفحة الموقع لا تفتح أصلًا** | النشر لم يكتمل | Vercel ← Deployments ← تأكد أن الحالة **Ready**، وإن كانت Error أرسل لي نص الخطأ |
| **رابط التهيئة يقول: هذه الصفحة معطّلة** | لم تُضف `SETUP_TOKEN` أو لم تُعِد النشر بعده | أضف المتغيّر في Vercel ← Settings ← Environment Variables ثم **Redeploy** |
| **رابط التهيئة يقول: الرمز (token) غير صحيح** | الرمز المكتوب في الرابط مختلف عن المتغيّر | تأكد أنك كتبت `token=Sohob-Setup-2026` بالحرف الكبير تمامًا |
| **صفحة التهيئة تقول: لم يتم ضبط رابط قاعدة البيانات** | `DATABASE_URL` غير موجود أو خطأ في كتابته | تأكد من الاسم بالحرف الكبير `DATABASE_URL`، وأن القيمة تبدأ بـ `postgresql://`، ثم **Redeploy** |
| **«جداول قاعدة البيانات غير موجودة بعد»** | المرحلة ٤ لم تُنفّذ | افتح رابط `/api/setup?token=...` مرة أخرى |
| **«كلمة مرور قاعدة البيانات غير صحيحة»** | الرابط منسوخ ناقصًا | ارجع لـ Neon ← Connection Details ← Copy، والصق في Vercel من جديد |
| **«قاعدة بيانات Neon كانت متوقفة وتعمل الآن»** | طبيعي في الخطة المجانية | انتظر ١٠ ثوانٍ وأعد تحميل الصفحة |
| **«اسم المستخدم أو كلمة المرور غير صحيحة»** | كتبت كلمة المرور خطأ | أعد الكتابة بدقة (كلمة المرور حسّاسة لحالة الأحرف) |
| **بعد الدخول تظهر رسالة خطأ 500** | غالبًا الجداول غير جاهزة | افتح `https://النطاق/api/health` واقرأ الرسالة العربية، ثم اتبع «الحل» المذكور فيها |
| **الصور لا تظهر** | لم تُرفع مجلد `public` | ارفع مجلد `public` إلى GitHub (داخل مجلد `public` يوجد مجلد `images`) |
| **زرت `/api/health` فظهر كلام تقني** | هذه صفحة التشخيص | اقرأ الحقول: `ok:true` = كل شيء سليم • `envSource` = من أي متغيّر أخذ الرابط • `tables` = حالة الجداول |

**صفحة التشخيص (هذه صديقتك):** افتح `https://النطاق/api/health` — تخبرك بالعربي: هل الرابط مضبوط؟ هل الاتصال يعمل؟ هل الجداول جاهزة؟ وما الحل.

---

## إذا نسيت كلمة المرور (الطريقة الرسمية لاسترجاعها)

1. ارجع إلى Vercel ← Settings ← Environment Variables وأضف من جديد:
   ```
   Key   : SETUP_TOKEN
   Value : Sohob-Setup-2026
   ```
2. اضغط **Redeploy** وانتظر انتهاءه.
3. افتح هذا الرابط (بدّل كلمة المرور الجديدة بما تريد، ويجب ٨ أحرف أو أكثر):
   ```
   https://النطاق/api/setup?token=Sohob-Setup-2026&resetPassword=كلمة-المرور-الجديدة
   ```
4. ستظهر صفحة تقول: **تم تغيير كلمة مرور "admin" بنجاح.**
5. سجّل الدخول بكلمة المرور الجديدة، ثم **احذف `SETUP_TOKEN`** من Vercel وأعد النشر (كما في المرحلة ٥) — مهم جدًا، لكي لا يستطيع أحد غيرك تغيير كلمة المرور.

---

## قواعد أمان لا تنسها

1. رابط Neon (`DATABASE_URL`) = كلمة سر نظامك كله. لا ترسله لأحد، ولا تضعه في الكود، ولا في رسالة واتساب عامة.
2. `SETUP_TOKEN` يُحذف بعد التهيئة (المرحلة ٥).
3. لا تجعل مستودع GitHub **Public** — اتركه Private.
4. لا تُعدّل ملف `.env.local` على جهازك ثم ترفعه إلى GitHub.
5. خُذ نسخة احتياطية من وقت لآخر: الإعدادات ← **تنزيل نسخة احتياطية الآن** (تحفظها على جهازك).

---

## أخيرًا: ماذا لو احتجت مساعدة؟

اكتب لي بالضبط:
1. أي خطوة كنت تنفّذ (مثلًا: «المرحلة ٤»).
2. **نص الرسالة كما هي** أو صورة شاشة لها.
3. ماذا توقّعت أن يحدث.

وأنا سأخبرك بالحل بالتفصيل. لا تُلغِ شيئًا ولا تعِد النشر عدة مرات قبل أن نسأل — غالبًا المشكلة كلمة مكتوبة خطأ أو متغيّر ناقص.